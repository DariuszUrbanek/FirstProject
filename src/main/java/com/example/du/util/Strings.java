package com.example.du.util;

public class Strings {

	/**
	 * @param s
	 * @return true if s is null or is empty, otherwise false
	 */
	public static boolean isBlank(String s) {
		return s == null || s.isEmpty();
	}
}
